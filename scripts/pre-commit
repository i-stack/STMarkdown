#!/bin/bash
#
# pre-commit — feature_2.0.0 分支不拦截直接提交
#
# 本分支（feature_2.0.0）允许直接 commit，故本 hook 始终放行。
# 仅 main 分支通过各自仓库内的 scripts/pre-commit 拦截直接提交。
#
# 注意：
#   - 本 hook 仅在本机生效，协作者需启用 core.hooksPath=scripts 才有。
#   - 远端分支保护规则仍是最终强控。

set -uo pipefail

# feature_2.0.0 不拦截任何提交
exit 0
